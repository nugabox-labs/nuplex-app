/**
 * 강제 업데이트 화면.
 * Phase 2 에서 remoteConfig.storeUrls 를 읽어 플랫폼별 스토어로 연결한다.
 */
document.querySelector<HTMLButtonElement>('#store')?.addEventListener('click', () => {
  // TODO(phase-2): remoteConfig.storeUrls[platform] 를 openExternal 로 연다
});
