<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>NUPLEX</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- Xcode 의 Background Modes → Remote notifications 에 해당한다.
	     사일런트 푸시(content-available)로 배지만 갱신할 때 앱이 깨어나려면 필요하다. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
	</array>
	<!-- canOpenURL 로 Plex 앱 설치 여부를 물어보려면 스킴을 미리 선언해야 한다.
	     기본 경로는 https 링크(Universal Links)라 이 선언 없이도 동작하지만,
	     ADR-003 의 실험 경로(plex:// 커스텀 스킴)를 켜려면 필요하다. -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>plex</string>
	</array>
	<!-- TV 캐스트(Plex Companion)는 같은 WiFi 의 사설 IP 에 평문 HTTP 로 붙는다.
	     docs/PLEX_CAST.md §7. 아래 둘이 없으면 캐스트가 조용히 실패한다.
	     · NSAllowsLocalNetworking — ATS 가 평문을 막는 것을 사설 대역에 한해 푼다.
	       전역 NSAllowsArbitraryLoads 와 달리 공용 인터넷은 그대로 막혀 있어
	       심사에서 사유를 요구받지 않는다.
	     · NSLocalNetworkUsageDescription — iOS 14+ 는 로컬 네트워크 접근에 사용자
	       동의를 받는다. 이 문구가 없으면 다이얼로그를 띄우지 못해 요청이 실패한다. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSLocalNetworkUsageDescription</key>
	<string>같은 WiFi 에 있는 TV 를 찾아 재생을 보내기 위해 사용합니다.</string>
	<!-- 수출 규정(Export Compliance) 응답을 미리 박아 둔다.
	     셸은 HTTPS 밖의 암호화를 쓰지 않으므로 면제 대상이다.
	     이걸 빼면 TestFlight 에 올릴 때마다 App Store Connect 에서 사람이 답할
	     때까지 빌드가 "처리 중" 에 멈춰 있다. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
